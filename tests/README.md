# Kiểm thử và bằng chứng

Lưu test SQL theo module và ảnh/kết quả ở `docs/evidence/` (không chứa thông tin nhạy cảm). Bắt buộc có `smoke`, `rollback`, `security`, `index-performance` và `concurrency`.

